# CLAUDE.md

# (User-managed content above)

# N3MemoryCore — recalled context for the current turn (auto-populated by hooks)
# Path resolves to ~/.n3mc/.memory/memory_context.md after `n3mc --init`.
# If you have not yet run `n3mc --init`, this @import will be a no-op.

@C:\Users\ttake\.n3mc\.memory\memory_context.md
